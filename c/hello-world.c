#include <stdio.h>

int main() {
    printf("Hello from C, Phantom!\n");
    return 0;
}

/* ===================================================
   🧠 C Functions & Concepts to Explore Later
   ===================================================

   scanf()         → Reads input from user (like "Enter a number:")
   fgets()         → Reads full lines of input safely (strings with spaces)
   fopen()         → Opens a file (returns a FILE pointer)
   fprintf()       → Writes formatted output to a file (like printf but to file)
   fclose()        → Closes an opened file
   malloc()        → Allocates memory during runtime (dynamic memory)
   free()          → Frees memory previously allocated with malloc
   struct          → Defines custom data types with grouped variables
   typedef         → Creates an alias for a data type
   for / while     → Loops used for repeating actions
   if / else       → Conditional logic for decision making
   switch/case     → Simplified branching logic (like menu systems)
   #define         → Creates macros/constants
   #include        → Adds system or custom headers for functions and types

   BONUS:
   makefile        → Used to compile multi-file projects automatically
   header files    → Store reusable function declarations (.h files)
*/
